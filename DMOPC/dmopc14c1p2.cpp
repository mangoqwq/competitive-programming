#include <bits/stdc++.h>

using namespace std;
typedef pair<int,int> pii;
typedef long long ll;

int main(){
    cin.tie(0); cin.sync_with_stdio(0);
    int w,l; cin >> w >> l;
    int s; cin >> s;
    cout << ((int)w/s) * ((int)l/s) << '\n';
}
