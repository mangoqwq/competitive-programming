#include <bits/stdc++.h>
//#pragma GCC optimize ("Ofast")

using namespace std;
#define ms(x,a) memset(x,a,sizeof x)
typedef long long ll;
typedef pair<int,int> pii;
const int mod=1e9+7,seed=131,MAXN=0;

ll dp[(1<<11)][16], w[(1<<11)][16]; 

int main(){
    cin.tie(0); cin.sync_with_stdio(0);
    int n,s; cin >> n >> s;
}
